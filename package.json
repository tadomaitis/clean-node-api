{
  "name": "clean-node-api",
  "version": "1.0.0",
  "main": "index.js",
  "reository": "https://github.com/tadomaitis/clean-node-api.git",
  "scripts": {
    "test": "jest --silent --colors --noStackTrace --passWithNoTests",
    "test:unit": "yarn test --watch -c jest-unit-config.js",
    "test:integration": "yarn test --watch -c jest-integration-config.js",
    "test:staged": "yarn test --findRelatedTests",
    "test:ci": "yarn test --coverage"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "Thiago Adomaitis <t.d.adomaitis@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "husky": "^4.2.5",
    "jest": "^26.1.0",
    "lint-staged": "^10.2.11",
    "standard": "^14.3.4"
  },
  "dependencies": {
    "validator": "^13.1.1"
  }
}
